[build]
  publish = ""
  command = "echo 'Netlify functions only; set webhook URL at /api/telegram_webhook'"

[dev]
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
